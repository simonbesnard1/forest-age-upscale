apiVersion: batch/v1
kind: Job
metadata:
  name: job
spec:
  template:
    spec:
      containers:
      - name: spearth
        command:
        - /bin/bash
        - -c
        - |
          source /venv/bin/activate
          pip install ./_submodules/dMRV-MethaneRice --upgrade
          python ./scripts/eddyCovariance_site_cube.py
        resources:
          requests:
            memory: "5000Mi"
            cpu: "1000m"
          limits:
            memory: "8000Mi"
            cpu: "1200m"
        volumeMounts:
        - mountPath: /data
          subPath: ec-cuber
          name: nfs-storage
      volumes:
      - name: nfs-storage
        persistentVolumeClaim:
          claimName: ec-cuber-pvc
          readOnly: false
