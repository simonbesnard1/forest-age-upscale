apiVersion: batch/v1
kind: Job
metadata:
  name: job
spec:
  template:
    spec:
      containers:
      - name: spearth
        command:
        - /bin/bash
        - -c
        - |
          source /venv/bin/activate
          pip install ./_submodules/dMRV-MethaneRice --upgrade
          python /scripts/MxD11A_site_cube.py
          #python /scripts/MCD43A_site_cube.py
        resources:
          requests:
            memory: "25000Mi"
            cpu: "1000m"
          limits:
            memory: "29000Mi"
            cpu: "1200m"
        volumeMounts:
        - mountPath: /data
          subPath: data-cuber
          name: nfs-storage
      volumes:
      - name: nfs-storage
        persistentVolumeClaim:
          claimName: cuber-pvc
          readOnly: false

